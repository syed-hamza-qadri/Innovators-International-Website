// Create a new layout file for the access section without the header
import type { ReactNode } from "react"

export default function AccessLayout({ children }: { children: ReactNode }) {
  return <>{children}</>
}
